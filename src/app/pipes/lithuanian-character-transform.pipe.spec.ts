import {LithuanianCharacterTransformPipe} from './lithuanian-character-transform.pipe';

describe('LithuanianCharacterTransformPipe', () => {
    it('create an instance', () => {
        const pipe = new LithuanianCharacterTransformPipe();
        expect(pipe).toBeTruthy();
    });
});
