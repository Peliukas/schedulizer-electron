import {LocalVariablesDirective} from './local-variables.directive';

describe('LocalVariablesDirective', () => {
    it('should create an instance', () => {
        const directive = new LocalVariablesDirective();
        expect(directive).toBeTruthy();
    });
});
